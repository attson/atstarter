# AGENTS.md

This repository keeps agent guidance in `CLAUDE.md`.

Before making changes:

1. Read `CLAUDE.md`.
2. Read the relevant long-lived spec under `docs/specs/` for the subsystem you touch.
3. Use `README.md` for product context and user-facing behavior, not as an implementation contract.

Do not duplicate generated project rules in this file. Update `CLAUDE.md` or `docs/specs/` instead.
